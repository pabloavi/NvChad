local colors = require("base46").get_theme_tb "base_30"

return {
  IblIndent = { fg = colors.line },
  IblWhitespace = { fg = colors.line },
  IblScope = { fg = colors.grey, bg = colors.one_bg2 },
}
